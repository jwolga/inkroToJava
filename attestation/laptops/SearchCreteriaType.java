package attestation.laptops;

public enum SearchCreteriaType {
    BRAND,
    RAM,
    SSD,
    PRICE,
    OPERATION_SYSTEM,
    COLOR,
    SEARCH // not an actual creteria, just to implement search
}
